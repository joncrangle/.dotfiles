-- FIX: relative line numbers disabled due to bug
-- require("relative-motions"):setup({ show_numbers = "relative" })
require("relative-motions"):setup()
require("starship"):setup()
require("zoxide"):setup({ update_db = true })
